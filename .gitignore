# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Environments
.env
.venv
env/
venv/
ENV/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# mypy / pytest / linting caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Secrets — never commit these
*.bat
*.lnk

# Test data / outputs (don't ship these to clients)
*.xlsx
*.dxf
*.dwg
*.tmp

# Backup / duplicate script versions
node_coordinate_calculator (1).py
node_coordinate_calculator (2).py

# --- Web server runtime state (don't commit) ---
server/instance/
server/jobs/
server/.env
server/.venv/
